apiVersion: v1
kind: Service
metadata:
  name: nginx-headless
  labels:
    app: nginx-headless
spec:
  clusterIP: None
  selector:
    app: nginx-headless
  ports:
    - port: 80
      name: web
      targetPort: 80
      protocol: TCP
